# Core Dependencies (REST API & Tuya Client)
PyYAML>=6.0
requests>=2.28.0
flask>=2.0.0
flask-cors>=3.0.0

# Optional: For GUI (install separately if needed)
# PyQt6>=6.0.0
# PyQt6-Charts>=6.0.0
# Install GUI: pip install -r requirements-gui.txt
